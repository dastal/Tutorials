# Functions #

Like normally in functions, the code inside the function is only executed when the function is called. The template of a function is as follows:

```
function functionName(input){
    function content;
}

functionName("input");
```

We can pass in as many parameters as we want.