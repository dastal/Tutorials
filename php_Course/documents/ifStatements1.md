# If Statements #

The template for an if statement is:

```
if (condition){
	do something;
} elseif (codndition) {
	do something;
} else {
	do something else;
}
```

Logical Operators:
- `&&`: and
- `||`: or
- `!`: not